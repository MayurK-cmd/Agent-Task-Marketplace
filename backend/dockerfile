FROM node:20-alpine

WORKDIR /app

# Install dependencies first (cached layer)
COPY package.json ./
RUN npm install

# Copy source
COPY . .

EXPOSE 3001

# Default: run migrations then start
# docker-compose overrides this with `npm run dev` for local
CMD ["sh", "-c", "npm run migrate && npm start"]